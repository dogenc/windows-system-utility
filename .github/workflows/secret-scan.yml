name: Secret scan

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  scan:
    name: Block private files and credentials
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Scan tracked repository files
        shell: pwsh
        run: ./scripts/Test-NoSecrets.ps1
